diff --git a/dist/acp-runtime/session.js b/dist/acp-runtime/session.js
index 6c1c946e7a464c36e91c75e732f3df9880895efa..5bf77891c006f9cc0cefa1307177be0e1be599f5 100644
--- a/dist/acp-runtime/session.js
+++ b/dist/acp-runtime/session.js
@@ -123,6 +123,7 @@ function mergeClientCapabilities(callbacks, extra) {
             writeTextFile: Boolean(callbacks.writeTextFile),
         },
         terminal: Boolean(callbacks.createTerminal),
+        ...(callbacks.createElicitation ? { elicitation: { form: {} } } : {}),
     };
     if (!extra)
         return inferred;
@@ -342,6 +343,16 @@ export class AcpSessionImpl {
             killTerminal: callbacks.killTerminal
                 ? async (params) => callbacks.killTerminal(params)
                 : undefined,
+            unstable_createElicitation: callbacks.createElicitation
+                ? async (params) => {
+                    try {
+                        return await callbacks.createElicitation(params);
+                    }
+                    catch {
+                        return { action: "cancel" };
+                    }
+                }
+                : undefined,
         };
     }
     #logInit(mode, startedAt, initializedAt) {
diff --git a/dist/acp-runtime/types.d.ts b/dist/acp-runtime/types.d.ts
index c72624d5c018cb1794a119b6c35744ad8cb97c85..8ef97a44e7512f8570e199275a1f5171e57ce593 100644
--- a/dist/acp-runtime/types.d.ts
+++ b/dist/acp-runtime/types.d.ts
@@ -47,6 +47,7 @@ export interface ClientCallbacks {
     releaseTerminal?(params: schema.ReleaseTerminalRequest): Promise<schema.ReleaseTerminalResponse | void>;
     waitForTerminalExit?(params: schema.WaitForTerminalExitRequest): Promise<schema.WaitForTerminalExitResponse>;
     killTerminal?(params: schema.KillTerminalRequest): Promise<schema.KillTerminalResponse | void>;
+    createElicitation?(params: schema.CreateElicitationRequest): Promise<schema.CreateElicitationResponse>;
 }
 export interface SessionOptions {
     agent: AgentSpec;
