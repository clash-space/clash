# Content-effect benchmark input Environment

`base-workspace-v1` is generated by
`benchmarks/generate-environment-workspaces.ts`. It is the same product
Workspace bundle format used by the functional track.
It uses the deterministic current-frontier `loro-shallow-snapshot` product
codec and is admitted through the real Workspace Host in the generator test.
