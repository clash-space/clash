{
  "name": "@clash-space/bridge",
  "version": "0.1.0-beta.21",
  "description": "Clash bridge — pair a local Claude Code (or other ACP agent) with the Clash web UI.",
  "private": true,
  "type": "module",
  "bin": {
    "clash-bridge": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup && pnpm bundle:plugin && pnpm bundle:assets",
    "bundle:plugin": "rm -rf dist/plugin && mkdir -p dist/plugin/clash-video-production && cp -R ../claude-code-plugin/commands ../claude-code-plugin/hooks ../claude-code-plugin/skills ../claude-code-plugin/index.mjs ../claude-code-plugin/openclaw.plugin.json dist/plugin/clash-video-production/",
    "bundle:assets": "rm -rf dist/assets && mkdir -p dist/assets && cp -R assets/. dist/assets/",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.20.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.0"
  }
}
