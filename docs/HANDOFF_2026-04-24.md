# Handoff — 2026-04-24

上下文和决策总结，给下一个 session 接着做。

---

## TL;DR

这个 session 修了 3 个视频编辑器的 bug，并决定把 `apps/web`（Next.js + Turbopack）迁走，换成 **Vite + React Router v7 + Cloudflare Vite plugin**（Workers 官方栈，2025-04-08 GA，稳定 1 年）。没动迁移代码，只做了决策和 spike 规划。

---

## 已修 bugs（都已生效）

### 1. Timeline 拖拽无法换 track

**根因**：`packages/remotion-ui/src/components/Timeline.tsx` 一个"优化"的 ref 缓存
（`viewportElRef`）在 `setLabelsPortalEl` 触发的重渲染里抓到了旧的 `.tracks-viewport`
DOM 节点；旧节点 detached 之后 `getBoundingClientRect()` 全 0，坐标换算变成
"item 在屏幕上的绝对 y"，`bandIdx = floor(y / 72)` 直接飙到 7、8（只有 2 条
track），路由全错。

**修法**：删 `viewportElRef` 缓存，drag 回调里每次现 `querySelector('.tracks-viewport')`。

**相关重构**：顺手把 `itemDragLogic.ts` 的 `buildPreview` 从 6 分支 zone 决策树
（A0/A/B/C/D/E + 源 track item 数特判）砍成单行 center-based：
`bandIdx = Math.floor(itemCenterY / trackHeight)` → 超出范围就建新 track，
否则目标就是 `tracks[bandIdx]`。这样和素材栏的 drop 心智一致（"鼠标在哪个 track 上就是哪个"），
再也不用补坑。

### 2. 多 item 时播放黑屏

**根因**：`packages/remotion-components/src/VideoComposition.tsx` 的 `ItemComponent`
把 `frame`（Sequence 相对帧，`useCurrentFrame()` 返回）和 `visibleFrom =
item.from`（合成绝对帧）做比较：

```ts
const isBeforeVisible = frame < visibleFrom;  // 相对帧 vs 绝对帧
```

对 `item.from = 0` 恰好两套值都是 0 蒙混过关；所有 `item.from > 0` 的 item
`hidden=true` 整段有效，外层黑底盖掉视频。所以"最左边（from=0）item OK，其他全黑"。

**修法**：`TrackComponent` 传 `visibleFrom` / `endFrame` 前转成 Sequence 相对帧；
`globalEndFrame` 参数改成预先算好的 `isGlobalEndItem` 布尔。

### 3. 前端 stuck-loading（async resolver 不重试）

**根因**：`useAsset` / `useSignedUrl` 失败后 `.catch(() => setState(undefined))`，
没有重试。任何一次 503（gateway 重启窗口、workerd 冷启动）都会永久卡住。

**修法**：加 `apps/web/lib/hooks/retryFetch.ts`，两处 hook 都换成 `fetchWithRetry`。
轻量策略：`maxAttempts=2`（1 次重试）、`capMs=1500`，只对 5xx/408/429/网络错重试，
4xx 直接透传。

---

## 坐标系文档（重要）

新建了 `packages/remotion-ui/TIMELINE_COORDINATES.md`，把 timeline/composition
里 **3 套独立坐标系**写清楚（`.tracks-viewport` 像素、合成绝对帧、Sequence 相对帧）
和这两次踩的坑复现路径。`AGENTS.md` 的 Critical Rules 也加了一条指向它。
**改 Timeline 或 composition 帧计算之前务必读一下。**

---

## 目前 dev 环境状况

- `make dev` 起着 gateway (:3000) + web (:3001) + api-cf (:8789)
- **web 已经手动换成 webpack**（`next dev` 无 `--turbopack`），因为 Turbopack
  serving 3.1MB 的 loro-crdt WASM 要 22 秒，且 `react-moveable` 之类的 dynamic
  chunk 随机 load 失败。Turbopack 在这个 codebase 不稳。
- `apps/web/package.json` 的 `dev` 脚本**还是** `next dev --turbopack --port 3001`，
  我是手动起的 `npx next dev --port 3001`。要么改脚本要么文件里留个 note。
- `apps/api-cf` 遇过 "Couldn't find a local dev session for the default entrypoint
  of service master-clash-api" 错——`.wrangler/tmp` 的 stale bundle 导致 wrangler
  build 失败 → workerd 退出 → gateway service binding 找不到上游。修法：
  `rm -rf apps/api-cf/.wrangler/tmp` + 重启 dev。

---

## 架构决策：迁到 Vite + React Router v7

### Why

1. **开发循环**：Turbopack 问题 + OpenNext 适配层各种奇葩 bug，日常被拖累
2. **架构契合度**：你 99% 是 client-side（canvas + 视频编辑 + Loro WASM），Next.js
   的 RSC / SSR 流基本没用上，OpenNext 只是把 Next 塞进不原生的 runtime
3. **CF 官方路径**：`@cloudflare/vite-plugin` 1.0 在 **2025-04-08 GA**，React
   Router v7 是一等公民。今天去用，踩到的是后人踩过的坑（1 年稳定窗口）
4. **主站内页想要 SSR**：用户明确表示要内页 SSR（登录门禁、SEO、首屏有内容），
   所以走 SPA + 静态 `index.html` 不够，需要 loader 能访问 `env.DB / R2 /
   service bindings`

### 目标架构

```
┌─────────────────────────────────────────────────────┐
│ Worker A: web (React Router v7 SSR on CF)            │
│  ├─ /, /projects, /projects/:id → SSR + hydrate      │
│  ├─ loader 里直接 context.cloudflare.env.DB 访问      │
│  ├─ [assets] binding 托管 client hydration bundle    │
│  └─ service binding → Worker B 调后端 API            │
├─────────────────────────────────────────────────────┤
│ Worker B: api-cf (现有 Hono, 保留)                    │
│  ├─ /api/v1/*  REST                                  │
│  ├─ /sync/*    Durable Object                        │
│  ├─ /agents/*  AI                                    │
│  └─ /assets/*  R2 signed URL                         │
└─────────────────────────────────────────────────────┘

apps/gateway 撤掉 —— 职能并入 web worker 的根 route 和 api-cf 的对应路由。
```

### 参考实现

`~/Proj/playheads/` 就是**活生生的 CF-native React 栈范本**，但是它是纯 SPA
（不是 SSR）。可以抄的东西：

- `apps/web/worker/index.ts` 才 6 行（`env.ASSETS.fetch(request)`）—— SPA 兜底
  由 wrangler 本身的 `not_found_handling = "single-page-application"` 配置做掉
- Gateway 是朴素 `export default { fetch }` + service bindings，不用 Hono，
  if-else 链读性好 bundle 小
- 每个 domain 独立 worker（`apps/web` / `apps/agent` / `apps/admin` /
  `apps/landing`），service binding 互相串
- `packages/auth` 抽出 better-auth + drizzle 共享（我们当前 better-auth 还耦合
  在 Next app 里）

**我们这次不抄纯 SPA 的部分**——playheads 不需要内页 SSR，你需要。所以用 RR v7
模板而不是 plain Vite React。

### Stack 具体选择

| 层 | 用啥 |
|---|---|
| build/dev | **Vite 6** + **`@cloudflare/vite-plugin`** |
| framework | **React Router v7 SSR 模板**（`npm create cloudflare@latest -- --framework=react-router`） |
| 客户端路由 | 本身就是 React Router v7 |
| 代码分割 | `React.lazy(() => import(...))` + `<Suspense>` 替 `next/dynamic` |
| client-only 模块 | `<ClientOnly>` 包 ReactFlow / Remotion Player / dnd-kit / loro-crdt（这些依赖 `window`，不能 SSR） |
| auth | `better-auth/hono` 挂在 api-cf 或 web worker 的 `/api/auth/*`，客户端继续用 `better-auth/react` |
| 资源 | 保留现有 R2 + signed URL，`<img>` / `<video>` 直接用 |
| 部署 | 原生 `wrangler deploy`，撤 `@opennextjs/cloudflare` |

---

## 迁移路线（增量，别 big bang）

**Phase 0（还没做）: Spike，1 天内能验证路径通**

```bash
cd /Users/xiaoyang/Proj/clash
npm create cloudflare@latest apps/web-spike -- --framework=react-router
cd apps/web-spike

# 1. 接 pnpm workspace：在 web-spike 的 package.json 加 workspace 依赖
#    "@master-clash/shared-types": "workspace:*"
#    "@master-clash/remotion-ui": "workspace:*"
#    等等

# 2. 确认 Vite 能解析 workspace 包的 TS 源码（通常自动，shared-types 已经
#    `"main": "src/index.ts"`，remotion-ui 同）

# 3. 移一条最简 route 进来（/projects 列表）:
#    - 写 loader(): context.cloudflare.env 拿 DB 查 projects
#    - 客户端 hydrate
#    - 验证 SSR HTML 里有数据 + client 水合后能交互

# 4. 改 wrangler.toml 加 service binding 到 api-cf:
#    [[services]] binding = "API" service = "master-clash-api"
#    loader 里 context.cloudflare.env.API.fetch(...) 调内部 API

# 5. pnpm build && wrangler dev 本地跑起来，验证:
#    - SSR 首屏
#    - /api/* 走 service binding 到 api-cf
#    - WebSocket sync/agents 路径怎么代理（大概也走 service binding）

# 6. 把 better-auth handler 搬一份进 web worker root route，
#    验证 Apple / Google login 流程
```

**通过 spike 的验收标准**：
- [ ] `/projects` 列表页 SSR 出 HTML 里包含用户的项目数据
- [ ] `/projects/:id` 能 SSR header，进而 `<ClientOnly>` 懒加载 ReactFlow + 视频编辑器
- [ ] Better-auth 登录 / 登出 / `getSession` 全流程 OK
- [ ] loro-crdt WASM 能加载（Vite 配置可能要处理 `.wasm` 资源）
- [ ] 和现有 `apps/api-cf` 同域，service binding 通信能用

**Phase 1: 核心页面迁移（2 周）**
- 把 `/projects`, `/projects/:id` 挪过去
- `next/dynamic` → `React.lazy`
- `useRouter` / `usePathname` → `useNavigate` / `useLocation`
- Link 组件

**Phase 2: 认证 + API routes（1 周）**
- Better-auth 从 `apps/web/app/api/better-auth/` 搬进去
- 其他 Next API route（如果有）迁移或撤掉（gateway 基本把 /api/v1 都转到 api-cf 了）

**Phase 3: 收尾（半周）**
- 撤 `apps/gateway`（职能并入新 web worker 和 api-cf）
- 撤 `apps/web`（旧 Next.js）
- 撤 `@opennextjs/cloudflare` 依赖
- 生产切流

总计 **3-4 周**。

---

## 给下一个 session 的具体动作

**优先级 1（必须做）**：把 `apps/web/package.json` 的 `dev` 脚本 `--turbopack`
去掉（或至少加条 README 提示），否则任何接手的人都会重新踩这个坑。

**优先级 2（验证决策）**：做 Phase 0 的 spike。1 天内给出结论：
- Vite + RR v7 + workspace 依赖的组合是不是真能无痛接 `packages/remotion-ui`
  这种重的包？
- Loro WASM 在 Vite plugin 下加载正常？
- Service binding 调 api-cf 生产和本地都通？

spike 不通的话**立刻停迁移**。通了再进 Phase 1。

**优先级 3（如果 spike 通）**：启动 Phase 1，建议开独立 branch `feat/migrate-vite-rr7`。

---

## 杂项备忘

- `apps/web` 目前手动启动的那个 Next 进程，如果下次重启 `make dev`，会被
  `make -j3 dev-web ...` 的 `pnpm dev`（带 turbopack）覆盖掉。记得改 script。
- Retry 策略对生产 5xx 短暂抖动有帮助，但对"workerd 挂了"这种 hard failure
  无能为力。生产部署不会遇到 `.wrangler/tmp` 的问题（那是本地 dev 独有）。
- `[CanvasPreview] allNodesMap built` 的 console.log 我一开始想删，发现文件
  里已经没这行了（有人删过，也加了 `backingAssetId` 映射）。没处理。
- 那次用户观察 Chrome 进程 133% CPU，大部分应该是 `<video>` decoding（多 item
  同时播）+ ReactFlow 频繁 layout，不是服务端问题。迁移后如果改成 SSR 首屏，
  重页面依然需要 `<ClientOnly>` 兜底，不会自动变快。

---

## 已改动的文件清单（这次 session）

修复类：
- `packages/remotion-ui/src/components/Timeline.tsx` — 删 viewportElRef 缓存
- `packages/remotion-ui/src/components/timeline/dnd/itemDragLogic.ts` — 砍 zone 决策树为 center-based
- `packages/remotion-components/src/VideoComposition.tsx` — Sequence 相对帧坐标 fix
- `apps/web/lib/hooks/retryFetch.ts` — 新建，fetch 重试工具
- `apps/web/lib/hooks/useAsset.ts` — 用 fetchWithRetry
- `apps/web/lib/hooks/useSignedUrl.ts` — 用 fetchWithRetry
- `apps/web/lib/hooks/useAsset.test.ts` — 测试调整

文档类：
- `packages/remotion-ui/TIMELINE_COORDINATES.md` — 新建，坐标系参考
- `AGENTS.md` — Critical Rules 加一条指向上面那个文档
- `docs/HANDOFF_2026-04-24.md` — 本文档

没 commit，全在工作区。跑 `git diff` 能看到。
